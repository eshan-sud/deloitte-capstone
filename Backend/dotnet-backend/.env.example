# ============================================================================
# ASP.NET CORE - Backend/dotnet-backend/
# ============================================================================
# (Copy to .env)
# MySQL Database Configuration (shares same DB with Spring)
ConnectionStrings__DefaultConnection="Server=localhost;Database=event_management;User=root;Password=password;""

# Server Configuration
ASPNETCORE_URLS="http://localhost:5107"
ASPNETCORE_ENVIRONMENT="Development"

# ASP.NET will start on http://localhost:5107
# API endpoints: http://localhost:5107/api/reports, /api/budgets, /api/expenses